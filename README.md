# 拾句 LineKeep

以 React Native + Expo 製作的書摘收藏 App，可在 iOS、Android 與 Web 執行。

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. 啟動 Expo：`npm start`
3. 使用 Expo Go 掃描 QR code，或執行 `npm run ios` / `npm run android`
